required:
  - act_name
  - amount
properties:
  act_name:
    oneOf:
      - $ref: ./all_activities_enum.yaml
      - type: array
        items:
          $ref: ./all_activities_enum.yaml
  location:
    $ref: ./geographies.yaml
  reference_product:
    type: string
  ef_cat:
    oneOf:
      - type: string
      - type: array
        maxItems: 2
        items:
          type: string
  amount:
    $ref: ./amount.yaml
