# yaml-language-server: $schema=../../../schemas/custom_activity.yaml

output:
  product: raw_data_CGM_project_final
  amount:
    value: 1
    unit: km

inputs:
  glider:
    act_name: market group for electricity, low voltage
    location: RER
    amount:
      value: 0.2
      unit: kWh
